#
# CoxIter: Computation of invariants of hyperbolic Coxeter groups
#
# Implementations
#
InstallGlobalFunction( CoxIter_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

